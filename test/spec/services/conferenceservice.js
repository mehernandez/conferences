'use strict';

describe('Service: conferenceService', function () {

  // load the service's module
  beforeEach(module('conferencesApp'));

  // instantiate service
/*  var conferenceService;
  beforeEach(inject(function (_conferenceService_) {
    conferenceService = _conferenceService_;
  }));


it('should attach a list of awesomeThings to the scope', function () {
var i = [];

  expect(i.length).toBe(0);
});
*/
});
